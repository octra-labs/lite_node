[package]
name = "rust_circle_factory"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true

[dependencies]
rust_circle_sdk = { path = "../rust_circle_sdk" }